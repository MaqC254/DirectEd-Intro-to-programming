#a function is initialized that prints hello with a name that is passed as an argument to it
def hello(name):
    print(f"hello, {name}")

#a function is initialized that prints goodbye with a name that is passed as an argument to it
def goodbye(name):
    print(f"goodbye, {name}")